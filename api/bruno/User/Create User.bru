meta {
  name: Create User
  type: http
  seq: 2
}

post {
  url: {{base_url}}/v1/users
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "username": "newuser",
    "email": "newuser@test.com",
    "password": "Test123!",
    "roles": ["viewer"]
  }
}
