meta {
  name: Assign Role
  type: http
  seq: 6
}

post {
  url: {{base_url}}/v1/users/:id/roles
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "role": "admin"
  }
}
