meta {
  name: Update User
  type: http
  seq: 4
}

patch {
  url: {{base_url}}/v1/users/:id
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "email": "updated@test.com",
    "status": "active"
  }
}
