meta {
  name: Order Update
  type: http
  seq: 8
}

patch {
  url: {{base_url}}/v1/orders/{{order_id}}
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
  X-Merchant-Id: {{merchant_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "dueDate": "2026-08-20",
    "note": "Perpanjang jatuh tempo"
  }
}
