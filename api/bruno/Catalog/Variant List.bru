meta {
  name: Variant List
  type: http
  seq: 12
}

get {
  url: {{base_url}}/v1/merchants/{{merchant_id}}/products/{{product_id}}/variants
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
