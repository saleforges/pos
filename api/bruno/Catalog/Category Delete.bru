meta {
  name: Category Delete
  type: http
  seq: 5
}

delete {
  url: {{catalog_base_url}}/v1/merchants/{{merchant_id}}/categories/{{catalog_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
