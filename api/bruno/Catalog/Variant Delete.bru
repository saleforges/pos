meta {
  name: Variant Delete
  type: http
  seq: 14
}

delete {
  url: {{base_url}}/v1/merchants/{{merchant_id}}/products/variants/{{variant_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
