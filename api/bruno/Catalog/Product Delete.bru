meta {
  name: Product Delete
  type: http
  seq: 10
}

delete {
  url: {{base_url}}/v1/merchants/{{merchant_id}}/products/{{product_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
