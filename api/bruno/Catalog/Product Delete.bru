meta {
  name: Product Delete
  type: http
  seq: 10
}

delete {
  url: {{base_url}}/v1/products/{{product_id}}
  body: none
  auth: bearer
}

headers {
  X-Merchant-Id: {{merchant_id}}
}

auth:bearer {
  token: {{access_token}}
}
