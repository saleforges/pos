meta {
  name: Product Item List
  type: http
  seq: 12
}

get {
  url: {{base_url}}/v1/products/{{product_id}}/items
  body: none
  auth: bearer
}

headers {
  X-Merchant-Id: {{merchant_id}}
}

auth:bearer {
  token: {{access_token}}
}
