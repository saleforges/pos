meta {
  name: Category Update
  type: http
  seq: 4
}

put {
  url: {{base_url}}/v1/categories/{{catalog_id}}
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
  X-Merchant-Id: {{merchant_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "Minuman Segar",
    "sort_order": 2
  }
}
