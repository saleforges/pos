meta {
  name: Product Get
  type: http
  seq: 8
}

get {
  url: {{catalog_base_url}}/api/v1/merchants/{{merchant_id}}/products/{{product_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
