meta {
  name: Product Item Branch Price Delete
  type: http
  seq: 10
}

delete {
  url: {{base_url}}/v1/product-items/{{item_id}}/branch-price?branchId={{branch_id}}
  body: none
  auth: bearer
}

headers {
  X-Merchant-Id: {{merchant_id}}
}

auth:bearer {
  token: {{access_token}}
}
