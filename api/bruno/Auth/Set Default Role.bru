meta {
  name: Set Default Role
  type: http
  seq: 8
}

put {
  url: {{base_url}}/v1/auth/me/default-role
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "userRoleId": {{role_id}}
  }
}
