meta {
  name: Logout
  method: POST
  url: {{base_url}}/api/v1/auth/logout
  body: json
}

headers {
  Content-Type: application/json
}

auth {
  type: bearer
  token: {{access_token}}
}

body:json {
  {
    "refresh_token": "{{refresh_token}}"
  }
}
