meta {
  name: Introspect
  method: POST
  url: {{base_url}}/api/v1/auth/introspect
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "token": "{{access_token}}"
  }
}
