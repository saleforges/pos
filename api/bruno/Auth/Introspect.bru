meta {
  name: Introspect
  type: http
  seq: 4
}

post {
  url: {{base_url}}/api/v1/auth/introspect
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "token": "{{access_token}}"
  }
}
