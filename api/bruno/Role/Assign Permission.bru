meta {
  name: Assign Permission
  type: http
  seq: 6
}

post {
  url: {{base_url}}/v1/roles/:id/permissions
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "permission": "reports.read"
  }
}
