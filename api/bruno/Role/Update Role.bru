meta {
  name: Update Role
  type: http
  seq: 4
}

patch {
  url: {{base_url}}/v1/roles/:id
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "description": "Updated description"
  }
}
