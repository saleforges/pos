meta {
  name: Remove Permission
  type: http
  seq: 7
}

delete {
  url: {{base_url}}/v1/roles/:name/permissions/:permissionId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
